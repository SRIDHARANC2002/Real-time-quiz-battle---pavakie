// Constants for the quiz application

const QUIZ_TIME_LIMIT = 30; // seconds per question
const MAX_PARTICIPANTS = 10; // max participants per room

module.exports = {
  QUIZ_TIME_LIMIT,
  MAX_PARTICIPANTS,
};
